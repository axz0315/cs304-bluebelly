<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Restaurant Profile Template</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>

<%# to-do: add href links to the tags and the reviews once review pages and search functionality are done. %>

<body>
    <h1><%= restaurant.name %></h1>
    <h2><%= price %> | <%= restaurant.addr %></h2>
    <p> phone #: <%= restaurant.phone %> | website: <%= restaurant.website%> </p>
    <button> Share Profile</button>

    <ul class = tags>
        <% restaurant.tags.forEach((tag) => {%>
            <li><button><%= tag %></button></li>
        <% }); %>
    </ul>

    //images

    <h2>Reviews</h2>

    <% reviews.forEach((review) => {%>
        <div class = review>
            <img src = <%= review.images[0] %> alt = 'review image'>
            <p> Rating: <%= review.rating%>/5 </p>
            <p> <%= review.text %> </p>
        </div>
    <% }); %>

    <!-- load local js -->
    <script src="/main.js"></script>
    <script>
        var uid = "<%= uid %>";
        if(uid !== 'unknown') {
            $("#logged-in").show();
            $("#not-logged-in").hide();
        } else {
            $("#logged-in").hide();
            $("#not-logged-in").show();
        }
    </script>
</body>
</html>
